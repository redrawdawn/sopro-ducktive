# Repository instructions

## GitHub push versioning

- Before every `git push` to GitHub for this repository, increase `APP_VERSION` in `lib/version.ts`.
- Every push must use a version number that has not been used by an earlier push, including follow-up or fix-up pushes.
- Include the version increase in the commits being pushed so the version shown inside Motive identifies that exact pushed state.
